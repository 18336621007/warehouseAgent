# 课程交接文档

> 最后更新：2026-08-10
> 项目路径：`D:\code\Project\test`  
> [返回文档索引](../文档索引.md)

---

## 一、项目目标

基于 SQL-MARS 的 Planner → Advisor/Seeker 思路，建设可上线的智能数仓问数系统。当前开发策略调整为功能优先：先在现有单表链路上实现安全的多表 Join 和分析性查询，再进行上下文、持久化、并发与部署优化。

当前能力：

- 自然语言问题 → 元数据识别 → 多轮澄清 → 方案确认 → 精确 Schema 加载 → SQL 生成与执行。
- Planner / Advisor / Seeker / Evaluator 多智能体协作。
- 库、表、字段三层 FAISS + 高质量示例长期记忆。
- Conversation / Topic / Request 三层业务身份、Graph Thread 执行身份与 Topic checkpoint 隔离。
- JSON Lines 结构化日志、请求链路追踪、状态变化记录和安全 `error_id` 异常边界。
- Web 前端和 CLI 共用 Graph State 业务状态。

---

## 二、当前多智能体链路

```text
CaptureMessage
  ↓
Planner：表/字段检索 + LLM 需求还原 + 完整度判断
  ├─ partial/none → Advisor 检索并追问（未确认口径由门禁拦截）
  ├─ full 或用户本轮解决歧义 → Advisor 提交完整方案
  └─ 用户接受完整 locked 方案 → confirm_query_plan → Seeker

Advisor：库/表/字段检索 → 解释歧义并追问；候选写入 pending_clarifications，下一轮由 Planner LLM 判断选择 + 程序白名单校验
Advisor：核对字段 → submit_query_plan
  ↓
lock_query_plan → QueryPlan(status=locked)
  ↓ 下一轮交给 Planner 判断确认或修改

Seeker：QueryPlanSchemaResolver
  ↓ 精确校验 database.table、单表约束和字段存在性
schema_context → GenerateSQL → ValidateSQL → ExecuteSQL → FinalAnswer → Evaluator
```

职责边界：

- Advisor 使用自适应模式：未解析口径由程序门禁拦截；当前轮完成元数据核验并解决歧义后可以直接形成 `locked` 方案，但不能授权执行。
- Planner 负责将合法 `locked` 方案升级为 `confirmed`。
- Seeker 只接受 `status=confirmed`，不通过向量检索重新选表选字段。
- QueryPlan、Resolver 和 SQL 链路已经支持当前多表场景；物理 ExecutionPlan 的进一步拆分保留为后续优化。

---

## 三、已完成功能

### 3.1 元数据与向量检索

| 组件 | 状态 | 说明 |
|---|---|---|
| Hive 原始元数据读取 | 已完成 | `metadata/hive_meta_provider.py` |
| LLM 元数据增强 | 已完成 | 库、表、字段写入 MySQL |
| `db_faiss_index` | 活动 | Advisor 库级引导 |
| `table_faiss_index` | 活动 | Planner/Advisor 表级检索 |
| `column_faiss_index` | 活动 | Planner/Advisor 字段级检索 |
| `example_faiss_index` | 活动 | Planner/Advisor/GenerateSQL Few-shot |
| `enriched_faiss_index` | 旧兼容资产 | 已退出当前 Graph Runtime，不再供 Seeker 使用 |
| `schema_faiss_index` | 对比资产 | 不进入当前主链路 |

### 3.2 QueryPlan 与精确执行

| 组件 | 文件 | 状态 |
|---|---|---|
| QueryPlan 契约 | `langgraph_app/state/query_plan.py` | 已完成 |
| 方案锁定/确认服务 | `langgraph_app/services/query_plan_service.py` | 已完成 |
| Advisor 双模式门禁 | `langgraph_app/graphs/advisor_graph.py` + `prompts/advisor_prompt.py` | 已完成，partial/none 无提交工具 |
| Advisor 方案提交 | `langgraph_app/tools/submit_query_plan.py` | 已完成，仅 plan 模式可用 |
| 精确 Schema Resolver | `langgraph_app/services/query_plan_schema_resolver.py` | 已完成 |
| Seeker Schema 节点 | `langgraph_app/nodes/retrieve_schema_node.py` | 已改为精确加载 |
| Seeker 子图 | `langgraph_app/graphs/seeker_graph.py` | 已移除旧 enrich 节点 |
| Runtime | `langgraph_app/runtime/graph_runtime.py` | 已移除通用 Seeker retriever |

### 3.3 State 与记忆

- `conversation_id`：一个前端对话，可包含多个 Topic。
- `topic_id`：一次独立查数任务。
- `request_id`：一次 HTTP/CLI 图调用。
- checkpoint `thread_id = conversation_id:topic_id`。
- `messages + add_messages` 保存当前 Topic 的标准消息历史。
- Web/CLI 只传身份和本轮输入，业务状态以 Graph State 为准。

### 3.4 Web 与 Evaluator

- Flask SSE 流式响应、闲聊/查询意图分离、SQL 展示和用户评分已完成。
- Evaluator 将评估写入 MySQL，高质量记录同步 `example_faiss_index`。

---

## 四、关键约定

### 4.1 上课与改码

1. 默认先讲设计、文件和代码，用户明确授权后才直接修改代码。
2. 不安排自动化测试课程，优先使用代码检查、日志和手动场景验证。
3. 不为临时兜底给 `HiveMetadataProvider` 增加 confirmed_plan 特例。
4. 不通过关键词硬编码判断确认或修改，由模型理解语义，程序负责契约校验。
5. 用户修改局部口径时优先基于原方案调整；方案根本错误时允许重新规划。

### 4.2 代码规范

- 使用中文注释。
- 修改代码不删除已有空行和注释，新代码添加简要注释。
- 避免无意义换行。
- Prompt 放在 `langgraph_app/prompts/`。
- 配置放在 `config/`。
- 禁止重新引入 `advisor_messages` 或 Web/Graph 双写 Topic 状态。

### 4.3 当前 Planner 配置

```python
MAX_HIGH_SIMILARITY_COUNT = 3
HIGH_SIMILARITY_THRESHOLD = 0.65
TABLE_SEARCH_K = 5
COLUMN_SEARCH_K = 7
EXAMPLE_SIMILARITY_THRESHOLD = 0.7
```

`HIGH_SIMILARITY_THRESHOLD` 和 `MAX_HIGH_SIMILARITY_COUNT` 保留为候选分布观测基线，用于日志、阈值校准和异常告警，不应直接覆盖 LLM 已结合上下文得出的 `completeness`。当前代码已不再用候选数量覆盖 `completeness`，仅作观测基线。

---

## 五、当前边界与技术债

- 已完成人工配置的表关系元数据和 SemanticMetadataProvider，JoinPlanner 可直接查询。；`tables` 尚未真正参与执行，下一阶段将通过 ExecutionPlan、关系元数据和 Join Planner 激活多表能力。
- `filters` 仍为字符串，Resolver 需要加载确认表完整物理 Schema。
- 跨库同名表已由 M1 `db.table` 白名单配置解决：`include_tables` 支持 `db.table` 精确指定，裸表名仅在同名唯一时生效。
- `SeekerState.schema_documents/schema_candidate_ids` 已退出活动链路但尚未删除。
- PlannerState、AdvisorState、SeekerState 仍重复声明继承来的 `confirmed_plan`，checkpoint 中仍是同一键。
- `AdvisorState` 当前未显式声明 `planner_entities/planner_reason`，父图进入 Advisor 子图时可能被 State Schema 过滤；待新增独立 `PlannerHandoffState` 契约。
- Advisor 方案模式若未调用 `submit_query_plan`，当前仍可能直接展示 LLM 文本；待增加协议校验和有限内部重试，只有获得真实 `locked_plan` 后才展示待确认方案。
- Planner 中“高相似候选数量强制 `full → partial`”的代码分支待删除，候选数仅保留在结构化日志和调优指标中。
- `MemorySaver` 仅适合单进程短期记忆，服务重启恢复尚未完成。
- GenerateSQL fallback 仍有单表、聚合和日期表达假设，需要在上线前继续收紧。

---

## 六、下一步课程

1. 已完成：第6-13.5课（契约修复、QueryPlan 扩展、关系元数据、覆盖分析与 Join 规划、AnalysisSpec 指标门禁、连续问答、元数据规模化接入与增量同步）。
2. 第14课：最小语义层对象模型（Metric / Dimension / Entity / BusinessTerm）。
3. 第15课：指标口径与物理绑定，接入 `semantic_default` 解析证据。
4. 第16课：标准语义文档与索引单元。
5. 第17-19课：BM25 关键词检索、向量语义检索升级与混合召回。

状态可观测性与全链路日志课程已完成，使用 `conversation_id/topic_id/request_id/graph_thread_id` 串联请求、节点、路由、状态与异常。具体使用方式见 [日志使用与问题排查指南](../指南/日志使用与问题排查指南.md)。

完整安排见 [后续课程规划](./后续课程规划.md)。

---

## 七、可写入简历的当前成果

- 多智能体 Text2SQL：Planner 模糊度识别、Advisor 分层澄清、Seeker 精确执行、Evaluator 反馈闭环。
- QueryPlan `locked → confirmed` 两阶段契约，分离方案形成与执行授权。
- Planner → Advisor `PlannerHandoffState` 显式交接契约，消除子图字段过滤。
- QueryPlan 已预留 `joins/field_sources/target_grain/metadata_version` 物理字段，为多表 Join 做准备。
- confirmed_plan 驱动的物理 Schema 精确校验，避免执行层二次语义召回导致表字段漂移。
- 库、表、字段三级增强元数据检索与高质量示例长期记忆。
- Conversation / Topic / Request 身份模型、checkpoint 隔离和统一消息记忆。
- JSON Lines 全链路结构化日志、ContextVar 并发上下文隔离和 `error_id` 安全异常定位。

详细设计：

- [多智能体 Text2SQL 系统架构](../架构/多智能体Text2SQL系统架构文档.md)
- [元数据与向量检索架构](../架构/元数据与向量检索架构.md)
- [State 与记忆系统架构](../架构/State与记忆系统架构.md)

## 十、当前交接状态

### 已完成

- 第12课：AnalysisSpec 与指标口径歧义门禁。
- Planner 只提取 `metric_mentions / dimension_mentions / analysis_type`，不替用户选物理字段。
- `MetricClarificationService.validate_user_selection` 对模型判断做白名单校验（field 命中 options、clarification_id 对齐），已去除 `_CHINESE_NUMBERS`/`_match_by_index` 硬规则；`MetricAmbiguityValidator` 校验字段必须落在真实候选或上轮已确认字段内；`semantic_default` 顺延到第14-15课接入。
- 多候选未选择时程序拦截锁定并生成候选选项；用户本轮解决歧义后同轮锁定，不增加重复确认。
- Advisor 确认消息确定性收敛：指标只展示已确认口径、字段描述用原始备注、表描述用表自带备注。
- 门禁候选收敛与排序配置化（`config/advisor.py`）。
- 日志升级为 trace 模型：事件携带 `seq/trace_id/span_id/parent_span_id`，可渲染请求级树形调用链。
- 新增 `scripts/trace_view.py` 树形查看工具，支持 `list/show/slow/filter`。
- 高频自由文本日志迁移为结构化事件：`tools.called / example.retrieved / plan.locked / advisor.mode`。

### 当前验证场景

“昨天新增订单最多的经销商的名称，状态，业务经理，联系方式”可完成：

1. Planner 判定 `partial` 并提取指标概念“新增订单”。
2. 门禁识别多候选，程序生成简洁选项（一句中文含义 + 字段名）。
3. 用户回复“指标字段为 new_order”后同轮锁定。
4. 确认消息只展示 `新增订单数（new_order）` 与 4 个维度字段。
5. 多表 Join、逐表 `pt_dt`、业务过滤隔离与 SQL 自动修复保持可用。

### 下一阶段

- 第13课：连续问答与延迟澄清恢复（已完成：去硬规则、pending 注册表、结果快照、两段式召回与模型受限精选、优秀案例存储改造；剩余待办评估关闭）。
- 第13.5课：元数据规模化接入与增量同步（M1 白名单配置化已完成：`agentTest/config/metadata.yaml` 支持 `db.table` 精确指定，`db/metadata_scope.py` 单一事实源；M2 schema 指纹增量采集已完成：`metadata_sync_state` + 字段级 diff；M3 变更同步已完成：`metadata_change_log` 审计 + `SchemaVectorStore.sync_documents` upsert/delete + `sync_metadata.py` 一键同步）。
- 第14课：最小语义层对象模型（Metric / Dimension / Entity / BusinessTerm）。
- 第15课：指标口径与物理绑定，接入 `semantic_default` 解析证据。
- 第16课：标准语义文档与索引单元。

### 接手注意事项

- 指标门禁配置集中在 `config/advisor.py`，修改后重启服务生效。
- 历史案例只参与候选排序，不能证明用户口径。
- 指标候选、选择解析和状态回写集中在 `metric_clarification_service.py`；Advisor Graph 只保留编排和最终确认展示。
- 排查问题优先使用 `python agentTest/scripts/trace_view.py show <request_id前缀>`，比手工过滤 JSON 高效。

### 第13课落地要点

- `pending_metric_clarification` 升级为 `pending_clarifications` 列表（`PendingClarification`：clarification_id/mention/question/options/status/created_request_id/last_active_request_id/resolved_value）。
- 去掉 `_CHINESE_NUMBERS` / `_match_by_index` 硬规则解析：PlannerOutput 新增 `user_selection`（selected/clarification_id/field/reasoning）与 `follow_up_mode`，判断归模型、校验归程序。
- Planner 模板改为【当前需求基线（滚动）+ 对话历史（最近 N 轮）+ 本轮输入 + 待澄清候选 + 已确认口径】；`original_question` 保留话题首轮原文，改写后的有效需求独立写入 `effective_query` 每轮滚动。
- 用户未选择同一概念时复用原 open pending（延迟澄清恢复）；选择通过白名单校验后 pending 清空、`explicit_user` 写入 `metric_resolutions`，Advisor 同轮锁定。
- `build_final_answer` 在查询成功/空结果后写入 `last_query_result` 结果快照（引用+预览+实体键），为结果追问提供结构化上下文。
- 用户询问口径区别时（`follow_up_mode=clarification_explanation`），Advisor 程序生成基于候选原始备注/别名/来源表的口径区别说明，pending 保持 open，不再重复展示“请选择”选项。
- 元数据来源分层：`enriched_columns` 新增 `meta_source`（`ddl_comment`=有 DDL 原始备注 / `llm_enhanced`=仅 LLM 增强别名）；澄清选项、口径区别说明、方案确认统一优先展示字段原始备注，无原始备注才回退增强别名并标注“系统推断别名”，避免增强元数据误导用户。
- 第13课收尾评估：原剩余待办（多 pending 冲突消解、结果追问路由、`follow_up_context`、pending 过期配置化）评估关闭——当前系统一次只澄清一个问题，结果追问依赖对话历史 + `last_query_result` 已正常，不新增结构化状态。
- 优秀案例存储：`original_question` 保留话题首轮原文、`effective_query` 独立滚动；Evaluator 入库原文/改写/确认方案三层；示例按问题去重（精确 hash + 语义 ≥0.9 且表/字段一致，高分优先）；FAISS 原文召回、`effective_query` 注入 Few-shot；MySQL 与 FAISS 的 `example_hash` 口径统一。
- 最终回答基准：`build_final_answer` 回答基准由 `original_question` 改为 `effective_query`（回退原文），已确认口径覆盖只保留 `confirmed_plan.measures` 中的指标，避免多轮追问后按首轮问题误判“数据不足以回答”。

## 十一、指标确认闭环与连续问答

### 已完成

- `AnalysisSpec` 增加 `pending_clarifications`（列表 + `PendingClarification`），首轮程序候选（含 `clarification_id`）写入 Topic State，候选编号在创建时固化。
- `MetricClarificationService.validate_user_selection` 对 Planner LLM 的 `user_selection` 做白名单校验，支持 `2 / 第二个 / 字段名 / 完整中文含义`（由模型判断，程序不写死映射）。
- Planner 结合【对话历史】+【待澄清候选】让模型判断用户选择，程序校验通过后写入 `explicit_user` 并清理 pending。
- Advisor 提交校验和无提交预校验统一回写解析结果。
- 已确认指标会注入 Advisor 上下文；LLM 未传 `concept_resolutions` 或提交错误 measure 时，程序按用户选择收敛。
- 候选重新召回或排序变化不会使上轮 resolved 字段失效。
- Advisor 的 `node.end.answer_summary` 记录最终用户可见回复。
- 指标确认与多表确认共 29 项测试通过。

### 当前边界

- 已支持单个 open pending 的延迟澄清恢复（用户先问口径区别、隔轮再回复编号），解释轮不会被视为选择；多 pending 冲突消解经评估不引入。
- 结果追问依赖对话历史 + `last_query_result` 快照，已可正常回答“第一名”“这些经销商”类追问。
- `follow_up_mode` 由 Planner 输出；`follow_up_context` 结构化上下文经评估不再引入。
- pending 过期策略经评估不引入，当前不自动过期。

### 第13课实施结果

1. 增加 `QueryResultSnapshot`（`last_query_result`），保存结果引用、列、预览行、实体键、行数和 confirmed plan。
2. 单个 pending 升级为 `pending_clarifications` 注册表（`active_clarification_id` 未引入）。
3. `follow_up_mode` 由 PlannerOutput 输出；`FollowUpContext` 与结果追问路由经评估不再引入。
4. 不再用确定性 Resolver 解析编号，改为 Planner LLM 判断 `user_selection` + 程序 `validate_user_selection` 白名单校验。
5. 查询成功后由 `build_final_answer` 节点生成结果快照。
6. 不新增独立 Agent，在现有 Supervisor、Planner 和澄清服务上迭代。

### 必测场景

- 查询后追问“第一名的业务经理联系方式”。
- 查询后说“换成近 7 天”，只修改时间槽位。
- 候选提出后先解释两轮，再回复“第二个”。
- 多 pending 时回复“第二个”，系统安全追问而非猜测。
- 新 Topic 不得读取旧 Topic 的候选或结果快照。
- 结果不存在或过期时不编造引用。

### 接手注意事项

- 编号必须以 pending 创建时的 options 为准，不能重新检索后重新编号。
- “第二个和第一个有什么区别”属于解释请求，不等于选择第二个。
- pending 过期策略经评估不引入；若未来需要，必须配置化，不能写死业务规则。
- 大结果只保存引用和有限预览，禁止全量写入 checkpoint。
- 连续问答仍必须经过指标门禁、字段来源、Join、逐表过滤和 SQL Guardrails。
